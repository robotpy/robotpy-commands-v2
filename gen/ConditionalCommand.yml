---

extra_includes:
- frc2/command/Command.h
- frc2/command/Subsystem.h

classes:
  ConditionalCommand:
    methods:
      ConditionalCommand:
        overloads:
          T1&&, T2&&, std::function<bool ( )>:
            ignore: true
          std::unique_ptr<Command>, std::unique_ptr<Command>, std::function<bool ( )>:
      Initialize:
      Execute:
      End:
      IsFinished:
      RunsWhenDisabled:
      InitSendable:
